#include<stdio.h>
void main(){
    int a,b;
    printf("enter the numbers:");
    scanf("%d %d",&a,&b);
    if (a>b){
        printf("a is the maximum");
    }
    else
    printf("b is the maximum");
}